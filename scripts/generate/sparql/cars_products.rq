# Carros / produtos com imagem, ano e país (modos QuandoLançou, De que país é,
# Adivinhe pela imagem). P18=imagem, P571=data de criação/início, P17=país.
# Use ?image (P18) para enriquecer licença/autor via API do Commons.
SELECT DISTINCT ?item ?itemLabel ?year ?image ?countryLabel ?countryCode WHERE {
  VALUES ?type { wd:Q3231690 wd:Q1361017 wd:Q42889 }   # modelo de automóvel, produto, veículo
  ?item wdt:P31 ?type ;
        wdt:P18 ?image ;
        wdt:P571 ?inception ;
        wdt:P17 ?country .
  ?country wdt:P298 ?countryCode .
  BIND(YEAR(?inception) AS ?year)
  FILTER(?year > 1700 && ?year <= YEAR(NOW()))
  SERVICE wikibase:label { bd:serviceParam wikibase:language "pt,en". }
}
LIMIT 5000
