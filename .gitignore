node_modules/
dist/
dist-ssr/
.vite/
coverage/

# Saída de build das Cloud Functions (gerada pelo esbuild)
functions/lib/

# Local env files (never commit Firebase keys / secrets)
.env
.env.local
.env.*.local

# Editor / OS
.DS_Store
*.log
npm-debug.log*
.idea/
.vscode/

# Generated data caches (large; built via scripts/generate)
scripts/generate/.cache/

# Base separada por minigame (gerada por scripts/generate/split.mjs nos hooks
# predev/prebuild a partir de src/data/items.json — não versionar).
src/data/mg/
src/data/meta.json

# Chaves privadas (assinatura iOS) — NUNCA versionar
*.pem
*.p12
*.p8
ios_cert_key*
