{
  "name": "quantum",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Quantum - app de quiz casual de estimativa e dedução (MVP)",
  "scripts": {
    "predev": "node scripts/generate/split.mjs",
    "dev": "vite",
    "prebuild": "node scripts/generate/split.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "data:build": "node scripts/generate/build.mjs",
    "data:split": "node scripts/generate/split.mjs"
  },
  "dependencies": {
    "firebase": "^10.12.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.3.1",
    "vitest": "^1.6.0"
  }
}
